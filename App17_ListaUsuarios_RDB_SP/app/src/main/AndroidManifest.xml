<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.nicholas.app17_listausuarios_rdbsp">

<application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_v2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_v2"
        android:supportsRtl="true"
        android:theme="@style/Theme.App17_ListaUsuarios_RDB_SP">
    <activity android:name=".CadastroActivity"
              android:label="@string/titulo_cadastro">

    </activity>
    <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>

            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>
</application>

</manifest>